---
phase: 05-cache-polish
plan: 06
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/app/routers/streaming.py
  - frontend/src/api/streaming.ts
  - frontend/src/hooks/useStreamingTranscribe.ts
  - frontend/src/components/realtime/RealTimePanel.tsx
  - frontend/src/components/fileupload/TranscribeQueue.tsx
  - frontend/src/components/fileupload/FileUploadPanel.tsx
autonomous: true
gap_closure: true
requirements: []

must_haves:
  truths:
    - "Real-time panel shows cache stats even when final transcription times out or fails, as long as a partial frame carried usage"
    - "Queued jobs display a remove button that immediately removes them from the queue"
  artifacts:
    - path: "backend/app/routers/streaming.py"
      provides: "usage field included in partial WebSocket frames from periodic_transcription"
      contains: "\"usage\": usage"
    - path: "frontend/src/api/streaming.ts"
      provides: "onPartial callback forwards usage data from partial frames"
      contains: "this.onPartial"
    - path: "frontend/src/hooks/useStreamingTranscribe.ts"
      provides: "partialUsage state persisting usage from partial frames"
      contains: "partialUsage"
    - path: "frontend/src/components/realtime/RealTimePanel.tsx"
      provides: "cache badge and stats row using finalUsage ?? partialUsage fallback"
      contains: "partialUsage"
    - path: "frontend/src/components/fileupload/TranscribeQueue.tsx"
      provides: "onRemove prop and X remove button per queued job row"
      contains: "onRemove"
  key_links:
    - from: "backend/app/routers/streaming.py:periodic_transcription"
      to: "frontend/src/api/streaming.ts:StreamingClient"
      via: "WebSocket partial frame containing usage JSON"
      pattern: "usage"
    - from: "frontend/src/api/streaming.ts"
      to: "frontend/src/hooks/useStreamingTranscribe.ts"
      via: "onPartial callback passing usage to hook"
      pattern: "onPartial.*usage"
    - from: "frontend/src/hooks/useStreamingTranscribe.ts"
      to: "frontend/src/components/realtime/RealTimePanel.tsx"
      via: "partialUsage state used as fallback"
      pattern: "partialUsage"
    - from: "frontend/src/components/fileupload/FileUploadPanel.tsx"
      to: "frontend/src/components/fileupload/TranscribeQueue.tsx"
      via: "onRemove prop wired to removeJob callback"
      pattern: "onRemove"
---

## Objective

Close two UAT-diagnosed gaps from Phase 5:
1. **Real-time panel cache stats not showing** (Test 4, Major) — when final transcription fails/times out, stats row doesn't render because `finalUsage` is null
2. **No remove button for queued jobs** (Test 6, Major) — `TranscribeQueue` has no way to remove queued items before processing

**Source:** `.planning/phases/05-cache-polish/05-UAT.md` gaps section

<execution_context>
@/home/haoli/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/haoli/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

## Code references for Gap 1 (cache stats fallback)

### backend/app/routers/streaming.py
- Line 61: `call_http_transcription` returns `(clean_text, lang, usage_dict)` — usage is already extracted
- Line 150: `clean_text, lang, _ = await task` — **BUG**: usage discarded with `_` in periodic loop
- Lines 152-158: `ws.send_json({"type": "partial", "text": ..., "language": ...})` — no usage field sent to frontend

### frontend/src/api/streaming.ts
- Line 27: `StreamingResponse.usage` already typed as `usage?: { prompt_tokens, completion_tokens, cache_read_tokens }` — no type change needed
- Line 78: `private onPartial: (text: string) => void` — **BUG**: callback doesn't accept usage
- Lines 203-204: `this.onPartial(frame.text ?? '')` — **BUG**: `frame.usage` not forwarded

### frontend/src/hooks/useStreamingTranscribe.ts
- Line 18: `finalUsage` in return interface
- Line 66: `useState<...>(null)` for finalUsage
- Lines 83-88: `onPartial` callback only sets `partialText`, no usage state tracked
- **Missing**: no `partialUsage` state to persist usage from partial frames

### frontend/src/components/realtime/RealTimePanel.tsx
- Line 64-76: Cache badge guarded by `finalUsage &&` — breaks when finalUsage is null
- Lines 90-101: Stats row guarded by `finalUsage &&` — breaks when finalUsage is null
- **Fix**: resolve `displayUsage = finalUsage ?? partialUsage` and use that for guards

## Code references for Gap 2 (remove button)

### frontend/src/components/fileupload/TranscribeQueue.tsx
- Lines 4-7: Props are `{ jobs: TranscribeJob[]; isProcessing: boolean }` — no `onRemove`
- Line 50-80: Job row renders filename, progress bar, status — no remove button

### frontend/src/components/fileupload/FileUploadPanel.tsx
- Line 25: `removeJob` available from `useTranscribeQueue()`
- Line 105-107: `activeJobs = jobs.filter(j => j.status !== 'complete' && j.status !== 'failed')`
- Line 152: `<TranscribeQueue jobs={activeJobs} isProcessing={isProcessing} />` — no `onRemove` wired
- Line 172: `removeJob` IS wired to `ResultCard` for completed results — established pattern to follow
</context>

<tasks>

<task type="auto">
  <name>Task 1: Propagate usage in partial frames (backend + frontend) + UI fallback</name>
  <files>
    backend/app/routers/streaming.py
    frontend/src/api/streaming.ts
    frontend/src/hooks/useStreamingTranscribe.ts
    frontend/src/components/realtime/RealTimePanel.tsx
  </files>
  <action>
  **Backend: Include usage in periodic partial WebSocket frames.**

  1. In `periodic_transcription()` at line 150: change `clean_text, lang, _ = await task` to `clean_text, lang, usage = await task`
  2. At lines 152-158, replace the send_json call to conditionally include usage:
     ```python
     payload = {"type": "partial", "text": clean_text, "language": lang}
     if usage:
         payload["usage"] = usage
     await ws.send_json(payload)
     ```
     The `StreamingResponse` type already has `.usage` — no frontend type change needed.

  **Frontend: Thread usage through partial callback → hook state → UI fallback.**

  3. In `frontend/src/api/streaming.ts`:
     - Change `onPartial` callback signature from `(text: string) => void` to `(text: string, usage?: StreamingResponse['usage']) => void`
     - At line 203-204: change `this.onPartial(frame.text ?? '')` to `this.onPartial(frame.text ?? '', frame.usage)`

  4. In `frontend/src/hooks/useStreamingTranscribe.ts`:
     - Add `partialUsage` state: `useState<{ prompt_tokens: number; completion_tokens: number; cache_read_tokens: number } | null>(null)`
     - Update `onPartial` callback: `(text, usage) => { setPartialText(text); setPartialUsage(usage ?? null); }`
     - Add `partialUsage` to `UseStreamingTranscribeReturn` interface
     - Add `partialUsage` to the hook return object
     - In `startRecording()`: also reset `setPartialUsage(null)` to clear stale data

  5. In `frontend/src/components/realtime/RealTimePanel.tsx`:
     - Add `partialUsage` to the destructuring of `useStreamingTranscribe()` (around line 10-22)
     - Derive resolved usage: `const displayUsage = finalUsage ?? partialUsage` (place after destructuring, before JSX)
     - Replace `finalUsage &&` guards with `displayUsage &&` (lines 64 and 90)
     - Replace `finalUsage.xxx` references with `displayUsage.xxx` in both cache badge (lines 67, 72-73) and stats row (lines 93-99)

  **Do NOT change:** the backend `do_final_transcription()` function, the WebSocket protocol for start/stop, or any behavior related to partial text streaming.
  </action>
  <verify>
    <automated>
      cd /mnt/c/0-中信湘雅/code/Qwen-ASR/frontend && npx tsc --noEmit
    </automated>
  </verify>
  <done>
    - streaming.py: `periodic_transcription` includes `"usage"` in partial frame payload when available
    - useStreamingTranscribe: exports `partialUsage` state, updated by onPartial callback, reset on startRecording
    - RealTimePanel: uses `displayUsage = finalUsage ?? partialUsage` for cache badge and stats row visibility
    - TypeScript compilation passes with no errors
  </done>
</task>

<task type="auto">
  <name>Task 2: Add remove button for queued jobs in TranscribeQueue</name>
  <files>
    frontend/src/components/fileupload/TranscribeQueue.tsx
    frontend/src/components/fileupload/FileUploadPanel.tsx
  </files>
  <action>
  1. **TranscribeQueue.tsx**:
     - Import `X` from `lucide-react` (join existing `Loader2` import)
     - Add `onRemove?: (index: number) => void` to `TranscribeQueueProps` interface
     - Destructure `onRemove` from props in the component function signature
     - Inside `jobs.map()`, after the status label div (line 68-79), append a conditional X button:
       ```tsx
       {onRemove && job.status === 'queued' && (
         <button
           onClick={(e) => { e.stopPropagation(); onRemove(index); }}
           className="p-1 text-gray-300 hover:text-red-500 transition-colors flex-shrink-0"
           title="Remove from queue"
           aria-label={`Remove ${job.file.name} from queue`}
         >
           <X className="w-3.5 h-3.5" />
         </button>
       )}
       ```
     - Only show for `queued` status (not `uploading`/`processing`) since those are already in flight

  2. **FileUploadPanel.tsx**:
     - Create a wrapper callback to convert `activeJobs` index to full `jobs` index (since `removeJob` expects the index in the full array):
       ```tsx
       const handleQueueRemove = useCallback(
         (activeIndex: number) => {
           const job = activeJobs[activeIndex];
           const fullIndex = jobs.indexOf(job);
           if (fullIndex !== -1) removeJob(fullIndex);
         },
         [activeJobs, jobs, removeJob],
       );
       ```
     - Wire it to TranscribeQueue: `<TranscribeQueue jobs={activeJobs} isProcessing={isProcessing} onRemove={handleQueueRemove} />`

  **Do NOT change:** the "Clear Results" button behavior, completed job removal (already in ResultCard via line 172), or the processQueue flow.
  </action>
  <verify>
    <automated>
      cd /mnt/c/0-中信湘雅/code/Qwen-ASR/frontend && npx tsc --noEmit
    </automated>
  </verify>
  <done>
    - TranscribeQueue renders X button for each `queued` job when `onRemove` prop is provided
    - FileUploadPanel wires `removeJob` through `handleQueueRemove` with index conversion
    - Clicking X on a queued job immediately removes it from the queue (activeJobs shrinks, job disappears from UI)
    - TypeScript compilation passes with no errors
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Queued job removal | Client-side only — no security boundary crossed; queue is local React state |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition |
|-----------|----------|-----------|-------------|
| T-05-06-01 | None | Gap closure — no new trust boundaries introduced | N/A — both fixes are UI/data-presentation only |
</threat_model>

<success_criteria>
1. RealTimePanel cache badge renders (green count or gray dash) when `finalUsage` is null but `partialUsage` exists
2. RealTimePanel stats row (prompt/completion/cached tokens) renders when `partialUsage` is available
3. Each queued job row in TranscribeQueue displays an X remove button
4. Clicking X removes the job from the queue and it disappears from the UI
5. No TypeScript compilation errors
</success_criteria>

<verification>
1. `cd frontend && npx tsc --noEmit` — passes with zero errors
2. Backend: `grep '"usage"' backend/app/routers/streaming.py` — confirms usage in partial payload
3. Frontend hook: `grep 'partialUsage' frontend/src/hooks/useStreamingTranscribe.ts` — confirms state exists
4. UI fallback: `grep 'displayUsage.*finalUsage.*partialUsage' frontend/src/components/realtime/RealTimePanel.tsx` — confirms fallback pattern
5. Remove button: `grep 'onRemove' frontend/src/components/fileupload/TranscribeQueue.tsx` — confirms prop wired
6. Manual: Start recording → stop → stats row renders even if final times out
7. Manual: Upload 3 files → click X on one queued job → it disappears, 2 remain
</verification>

<output>
After completion, create `.planning/phases/05-cache-polish/05-06-SUMMARY.md`
</output>
