---
phase: 02-frontend-connection-test
plan: 02
type: execute
wave: 2
depends_on: [02-01-PLAN.md]
files_modified:
  - frontend/src/api/health.ts
  - frontend/src/components/connection/ConnectionTestPanel.tsx
  - frontend/src/components/connection/HealthStatus.tsx
  - frontend/src/components/connection/LatencyChart.tsx
  - frontend/src/hooks/useHealth.ts
  - frontend/src/App.tsx
autonomous: true
requirements: [CONN-01, CONN-02, CONN-03, CONN-04, UI-03, UI-04]
user_setup: []

must_haves:
  truths:
    - "Connection test panel shows live status indicator (green when connected, red when disconnected)"
    - "Model name is displayed from health check response"
    - "Latency in milliseconds is shown and updates on each refresh"
    - "Manual refresh button triggers a new health check immediately"
    - "Auto-polling refreshes health status periodically (5s interval)"
    - "Error states show user-friendly messages with actionable guidance"
    - "Layout is responsive at 1920x1080 and 1366x768 viewports"
  artifacts:
    - path: "frontend/src/hooks/useHealth.ts"
      provides: "Custom hook for health polling with auto-refresh, manual trigger, and latency history"
      exports: ["useHealth"]
    - path: "frontend/src/api/health.ts"
      provides: "API client function for GET /api/health"
      exports: ["checkHealth"]
    - path: "frontend/src/components/connection/HealthStatus.tsx"
      provides: "Visual status indicator component (green/red dot + text)"
      min_lines: 30
    - path: "frontend/src/components/connection/ConnectionTestPanel.tsx"
      provides: "Complete connection test panel with status, model info, latency, refresh controls"
      min_lines: 50
  key_links:
    - from: "frontend/src/components/connection/ConnectionTestPanel.tsx"
      to: "PATCH:8000/api/health"
      via: "frontend/src/api/health.ts"
      pattern: "fetch.*api/health"
    - from: "frontend/src/hooks/useHealth.ts"
      to: "frontend/src/api/health.ts"
      via: "import and polling interval"
      pattern: "import.*checkHealth"
    - from: "frontend/src/App.tsx"
      to: "frontend/src/components/connection/ConnectionTestPanel.tsx"
      via: "React component render for connection tab"
      pattern: "import.*ConnectionTestPanel"
---

<objective>
Build connection test panel with live status indicator, model info display, latency measurement, and error handling. Wire into tab navigation from Plan 01.
</objective>

<execution_context>
@/home/haoli/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/haoli/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md

# Phase 2 decisions
@.planning/phases/02-frontend-connection-test/02-CONTEXT.md

# Backend health API contract
@backend/app/routers/health.py
@backend/app/errors.py

# Plan 01 output (tab navigation, App.tsx structure)
@frontend/src/App.tsx
</context>

<interfaces>
<!-- Backend API contract for health endpoint -->

From backend/app/routers/health.py:
```typescript
// GET /api/health response (success)
interface HealthResponse {
  status: "ok";
  model: string;      // e.g. "/bmcp_lvm_fs/cusa/models/Qwen3-ASR-1.7B"
  latency_ms: number; // round-trip latency in ms
}

// GET /api/health response (error cases)
interface HealthErrorResponse {
  status: "error";
  message: string;    // e.g. "No models loaded on vLLM"
  code: number;       // 503
}

// FastAPI structured error (D-08, from errors.py)
interface APIError {
  error: string;     // "timeout" | "connection_failed" | "internal_error"
  message: string;   // human-readable
  code: number;      // 504 | 503 | 500
}
```

<!-- Phase 1 plan dependencies -->
From frontend/src/types/tab.ts (created by Plan 01):
```typescript
export type TabId = 'connection' | 'fileupload' | 'realtime' | 'settings';
```

From frontend/src/lib/utils.ts (created by Plan 01):
```typescript
import { type ClassValue, clsx } from "clsx";
import { twMerge } from "tailwind-merge";
export function cn(...inputs: ClassValue[]) { return twMerge(clsx(inputs)) }
```
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Health API client + useHealth polling hook</name>
  <files>frontend/src/api/health.ts, frontend/src/hooks/useHealth.ts</files>
  <behavior>
    - Test 1: checkHealth() returns typed data with status, model, latency_ms on success
    - Test 2: checkHealth() throws structured error on failure (timeout, connection_failed)
    - Test 3: useHealth hook initializes with polling on mount at 5s interval
    - Test 4: useHealth manualRefresh() triggers immediate health check
    - Test 5: useHealth aborts pending poll on unmount (cleanup effect)
    - Test 6: useHealth maintains latencyHistory array (last 10 readings)
  </behavior>
  <action>
    Build health-checking layer per D-13 (polling-based refresh) and D-12 (status indicator data):
    1. Create `src/api/health.ts`:
       - Type definitions: `HealthResponse`, `HealthErrorResponse`, `APIError`
       - `export async function checkHealth(): Promise<HealthResponse | HealthErrorResponse>`:
         - Fetch GET `/api/health` (via Vite proxy, no CORS)
         - Return parsed JSON on 200 (whether status is "ok" or "error" from vLLM)
         - On network error / timeout, throw with structured error shape
         - Include AbortController for cancellability
    2. Create `src/hooks/useHealth.ts`:
       - Returns: `{ status, model, latencyMs, isChecking, latencyHistory, lastChecked, error, manualRefresh, pollingInterval }`
       - Internal state: `data`, `error`, `isChecking`, `latencyHistory` (array of last 10 latency readings)
       - Polling: `useEffect` with `setInterval(checkHealth, interval)` default 5000ms
       - On mount: immediate check + start polling
       - On unmount: `clearInterval`, `abortController.abort()` to cancel in-flight requests
       - `manualRefresh()`: calls `checkHealth()` immediately, updates state
       - Error handling: captures error response, sets error state, clears on next success
       - Latency history: push new latency_ms to array, keep last 10 entries, used for trend display
  </action>
  <verify>
    <automated>cd frontend && grep -c "setInterval\|AbortController\|latencyHistory" src/hooks/useHealth.ts</automated>
  </verify>
  <done>
    - Health API client with typed checkHealth() function
    - useHealth hook with auto-polling (5s), manual refresh, abort on unmount
    - Latency history tracking (last 10 readings)
    - Error state management with clearing on next success
    - All types properly defined and exported
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Connection test panel + HealthStatus indicator + Wire to App</name>
  <files>frontend/src/components/connection/ConnectionTestPanel.tsx, frontend/src/components/connection/HealthStatus.tsx, frontend/src/components/connection/LatencyChart.tsx, frontend/src/App.tsx</files>
  <behavior>
    - Test 1: HealthStatus renders green dot + "Connected" when status is "ok" (D-12)
    - Test 2: HealthStatus renders red dot + error message when status is "error"
    - Test 3: ConnectionTestPanel shows model name from health response (CONN-02)
    - Test 4: ConnectionTestPanel shows latency in ms (CONN-03)
    - Test 5: Manual refresh button is present and clickable (D-13)
    - Test 6: Mini sparkline/bar chart shows latency history trend
    - Test 7: Error states show actionable guidance messages (UI-03)
    - Test 8: ConnectionTestPanel renders in App.tsx when 'connection' tab is active
  </behavior>
  <action>
    Build connection test panel per D-12 (large status indicator) and CONNECT requirements:
    1. Create `src/components/connection/HealthStatus.tsx`:
       - Props: `{ status: 'ok' | 'error' | 'checking'; message?: string }`
       - Large visual indicator: green circle (connected) / red circle (disconnected) / pulsing yellow (checking)
       - Status text below indicator: "Connected", "Disconnected", "Checking..."
       - Animated transition between states (CSS transitions, 200ms)
       - Per D-12: always visible at top of panel, prominent size
       - Use lucide-react icons: CheckCircle2 (green), XCircle (red), Loader2 (pulse/spinner)
    2. Create `src/components/connection/LatencyChart.tsx`:
       - Props: `{ data: number[] }` — array of latency readings in ms
       - Simple horizontal bar/sparkline visualization (no external chart library)
       - Each bar represents one reading, height proportional to latency
       - Color coding: <100ms green, 100-500ms yellow, >500ms red
       - Compact horizontal layout showing last 10 readings
       - Tooltip on hover showing exact ms value
    3. Create `src/components/connection/ConnectionTestPanel.tsx`:
       - Uses useHealth hook for data
       - Layout:
         - Top: HealthStatus indicator (large, prominent per D-12)
         - Model info section: model name displayed (CONN-02)
         - Latency section: current latency_ms + LatencyChart showing history (CONN-03)
         - Controls: "Refresh Now" button triggers manualRefresh() (D-13)
         - Last checked timestamp
       - Error handling (UI-03):
         - timeout error → "API request timed out. Check connectivity and try again."
         - connection_failed error → "Cannot reach vLLM server. Check API URL and network settings."
         - internal_error → "Server error. Try refreshing or check Settings."
       - Per D-16: also trigger sonner.toast for persistent errors (not just status display)
       - Responsive layout adjustments for D-17 (1366x768: stack vertically; 1920x1080: side-by-side where appropriate)
    4. Update `src/App.tsx`:
       - Import ConnectionTestPanel
       - Wire 'connection' tab to render ConnectionTestPanel
       - Replace existing placeholder for connection tab
  </action>
  <verify>
    <automated>cd frontend && grep -c "HealthStatus\|ConnectionTestPanel\|LatencyChart" src/components/connection/ConnectionTestPanel.tsx src/components/connection/HealthStatus.tsx src/App.tsx</automated>
  </verify>
  <done>
    - HealthStatus component with green/red/status indicator (D-12, CONN-04)
    - LatencyChart bar/sparkline visualization (CONN-03)
    - ConnectionTestPanel with model info, latency, refresh button, error messages
    - Error messages are user-friendly with actionable guidance (UI-03)
    - Wired into App.tsx for 'connection' tab
    - Responsive layout works at 1920x1080 and 1366x768 (UI-04)
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → FastAPI Backend | Health check reads-only; no data modification |
| Polling Interval → Server Load | 5s interval could cause load if many users |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-05 | DoS | Health polling (5s) | mitigate | Single-tab app, 1 user; interval configurable; abort on unmount prevents orphaned requests |
| T-02-06 | Information Disclosure | Model path in response | accept | Internal demo; model path is non-sensitive infrastructure info |
| T-02-07 | Manipulation | Latency spoofing | accept | Latency measured server-side (backend timing); frontend only displays, does not compute |
| T-02-08 | DoS | Unbounded latency history | mitigate | Cap latencyHistory at 10 entries; bounded memory usage |
</threat_model>

<verification>
- `cd frontend && npm run build` succeeds with 0 errors
- Connection test panel shows status indicator when health endpoint is reachable
- Status indicator is green (connected) or red (disconnected) based on backend response
- Model name displayed from health check response
- Current latency shown in milliseconds
- Manual refresh button works (triggers immediate health check)
- Latency history chart shows last 10 readings
- Error messages are user-friendly and actionable
- Responsive layout works at 1920x1080 and 1366x768
</verification>

<success_criteria>
1. Connection test panel renders in 'Connection Test' tab with status indicator
2. Status shows green (connected) or red (disconnected) with model name
3. Latency displayed in milliseconds with refresh button
4. Auto-polling refreshes every 5 seconds, manual refresh triggers immediately
5. Error messages provide actionable guidance (e.g., "Check API URL and network")
6. Layout is responsive on desktop (1920x1080 and 1366x768)
</success_criteria>

<output>
After completion, create `.planning/phases/02-frontend-connection-test/02-02-SUMMARY.md`
</output>
